import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const weatcherSlice = createSlice({
  name: sliceName,
  initialState,
  reducers: {},
});

export const {} = weatcherSlice.actions;
export default weatcherSlice.reducer;
